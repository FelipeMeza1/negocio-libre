export interface Cliente {
    id: number,
    rut: number,
    nombre: String,
    email: String,
    password: String,
    rol: number
}

export interface Clientes {
    rut: number,
    nombre: String,
    email: String,
    password: String,
    rol: number
}